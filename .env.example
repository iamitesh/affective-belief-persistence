# Optional model-provider credentials. The offline smoke run does not use them.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
HUGGING_FACE_HUB_TOKEN=

# Optional artifact location. Defaults to ./runs when unset.
ABP_RUNS_DIR=
