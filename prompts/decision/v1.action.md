Select exactly one allowed action using only the supplied model input.
Return one JSON object matching the supplied schema. The action identifier and
resource cost must exactly match the allowed-action contract. Cite only memory
and evidence identifiers present in the input. If included, the decision
rationale must be a concise, release-safe evidence summary.
